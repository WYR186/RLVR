# EAAJ Pilot 完全理解指南（中文）

写给 Aaron 自己：从零开始把这三次实验讲透——为什么做、怎么做的、每个设计
选择背后的理由、三次执行各自的意义、结果怎么读、总体做得怎么样。
读完这份文档，你应该能在不看任何笔记的情况下向 Tommy、Madhur 或任何队友
解释清楚整个 pilot。

日期：2026-07-15。所有数字来自已提交的产物（evidence pack 可核查）。

---

## 第一部分：大图景 —— 我们团队到底在研究什么

### 1.1 背景问题

现在训练推理大模型的主流方法之一是 **RLVR**（Reinforcement Learning with
Verifiable Rewards，可验证奖励的强化学习）：让模型做数学题，答案对了给
奖励 1、错了给 0，用强化学习（比如 GRPO）更新模型。

实际工程中，RLVR 往往**分多个阶段**：先在任务 A 上 RL 一轮（Stage A），
再在任务 B 上 RL 一轮（Stage B），可能还有 C、D……

一个常见且昂贵的失败模式是：**Stage B 停滞（stall）**。模型在 Stage A
练得好好的，dashboard 上一切正常，但到了 Stage B 突然学不动了——而你
发现这一点时，Stage B 的算力已经烧完了。对大模型训练来说，这是几万到
几十万美元级别的浪费。

### 1.2 团队的核心猜想

团队的赌注（proposal "Seeing the Stall Coming"）是：

> Stage A 的训练会让模型**悄悄失去可塑性**（plasticity）——学新东西的
> 内在能力。这种流失在 dashboard 信号（reward 曲线、KL、梯度范数、熵）
> 上看不出来，但在模型**内部激活的几何结构**里留有痕迹。如果我们能读出
> 这个痕迹（记为 Q），就能在 Stage A 进行中提前预警"Stage B 会停滞"，
> 而且比 dashboard 信号预警得**更早**。

两个候选的 Q 指标：

- **Effective rank（有效秩，主指标）**：把 512 个 prompt 喂给模型，取
  某一层最后一个 token 的隐状态，堆成 512×d 的矩阵，做 SVD 得到奇异值
  谱，算 `erank = exp(−Σ p_i log p_i)`（谱熵的指数）。直觉：**模型内部
  表征还"张开"多少个有效维度**。维度丰富 = 还有学新东西的余地；谱塌缩
  到少数几个方向 = 表征变僵了。
- **Dormant-neuron fraction（休眠神经元比例，副指标）**：MLP 激活层里，
  平均激活幅度低于阈值（τ ∈ {0.025, 0.1}）的神经元占比。直觉：**多少
  神经元已经"躺平"不参与计算了**。躺平的越多，网络的可用容量越少。

### 1.3 一个贯穿全文的比喻

把这个项目想成**造一台体检仪器**：

| 比喻 | 对应实物 |
|---|---|
| 病 | 可塑性流失（学新东西的能力下降） |
| 病发作 | Stage B 停滞（学不动了） |
| 血检指标 | Q（effective rank、dormant fraction） |
| 体能测试 | 固定预算适应实验（真让模型学个新任务，看能学多少） |
| 现有的听诊器 | dashboard 信号（reward 斜率、KL、梯度范数、熵） |

团队要证明的是：**血检指标（Q）比听诊器（dashboard）更早发现病。**

### 1.4 Madhur（mentor）的红线：framing 约束

Madhur 明确指出："可塑性"没有公认的单一标量，所以**永远不许说
"RLVR 降低了模型的学习能力"**这种话。可以测、可以说的是一个操作化定义：

> RLVR 是否降低模型在**固定预算**下对 held-out 任务家族的**未来适应力**
> （fixed-budget future adaptability），对照 checkpoint-0 或 KL 正则化的
> checkpoint？

所以我们记录的每个 outcome 都必须是固定预算量：任务、预算、baseline
三者事先声明。这不是文字游戏——它把一个哲学问题（"能力"是什么）换成了
一个可测量、可证伪的问题（"同样练 50 步，谁提分多"）。

---

## 第二部分：RQ1 —— 这次 pilot 服务的研究问题

### 2.1 RQ1 的准确表述

RQ1 是 proposal 的第一研究问题（存在性/相关性问题）：

> **在 Stage A 的 checkpoint t 上测得的 Q，是否与"从 checkpoint t 出发的
> 固定预算适应收益"相关？**

拆成 X 和 Y：

- **X（预测方）** = Q(checkpoint)。测起来便宜：一次前向传播就够，不用
  训练。如果它有预测力，就能当"随时可看的仪表盘"。
- **Y（被预测方）** = svamp_delta = 从该 checkpoint 出发做恰好 50 步
  SVAMP 训练后，准确率提升了多少。测起来贵：每测一次要真的训练一遍。

RQ1 问的就是：**便宜的 X 能不能替代昂贵的 Y 做预警？** 落到统计上：
Spearman rho(erank_L12, svamp_delta) 是否显著为正（erank 高的 checkpoint
适应收益也高）。

### 2.2 为什么 RQ1 是整个项目的第一步

项目的最终交付物是一个"停滞探测器"，要和 dashboard 基线比**预警提前量**
（lead time）。但在造探测器之前，必须先回答一个更基本的问题：**Q 和
未来适应力之间到底有没有关系？** 如果连相关性都不存在，后面全免谈。
RQ1 就是这个"先决条件检查"，pilot 就是 RQ1 的第一次小规模尝试。

### 2.3 pilot 与正式实验的关系

proposal 设想的"危险区"是 1.5B 模型全参数微调；pilot 用 0.5B 是 Colab
预算的妥协。**pilot 的定位从头就是可行性验证（feasibility pilot），
不是正式证据。**它的三个任务：

1. 把全流程管线跑通（训练 → 测 Q → 固定预算适应 → 分析）；
2. 摸清各环节的噪声和成本；
3. 为正式实验的设计提供参数（要多少种子、多大评估集、多强的 Stage A）。

记住这个定位——后面评价结果时全靠它。

---

## 第三部分：实验设计逐条拆解 —— 每个选择为什么是这样

配方只有一份：`eaaj-pilot/pilot_config.json`（预注册，跑之前就冻结）。
下面按流程顺序拆解每个设计选择。

### 3.1 Stage A：制造"训练里程"不同的模型快照

**做法**：GRPO 训练 Qwen2.5-0.5B，512 道冻结的 GSM8K 题，exact-answer
二值奖励（最终数字对=1，错=0），200 次更新，lr 1e-6，KL β=0，温度 0.7，
每题 8 个生成，每次更新 64 个 completion。沿途在第 0/25/50/100/200 次
更新处保存完整 checkpoint。

**为什么这么设计**：

- **为什么是 GRPO + 数学题 + 二值奖励**：这就是业界 RLVR 的标准形态，
  我们要研究的现象就发生在这种训练里。GRPO 选 TRL 实现（verl 对这个
  规模是杀鸡用牛刀）。
- **为什么存 5 个 checkpoint（0/25/50/100/200）**：这 5 个快照就是我们
  的"实验对象"。理论预期：训练越久，可塑性流失越多。5 个点覆盖
  从"完全没训"（ckpt-0 = base 模型，天然的对照组）到"训完全程"。前密
  后疏（0/25/50 vs 100/200）是因为如果流失发生，早期变化率通常更大。
- **为什么 KL β=0**：β=0 意味着不约束模型偏离初始分布——这是 proposal
  说的"危险区"配置，最容易诱发可塑性流失。Madhur 提的 KL 正则对照
  （β>0）是挂起的待决项，pilot 按更便宜的默认值实现并记录在案。
- **为什么 lr 1e-6**：Tommy 的 spec 把超参数留白（"suggestions"），这是
  实现时选的保守默认值。**事后看这是 pilot 最大的教训之一**——这个
  学习率 × 200 步太温和，几乎没有伤到模型（详见第五部分）。

### 3.2 Q 测量：让数字跨 checkpoint 可比的三道锁

**做法**：每个 checkpoint 上，用**冻结的 512-prompt probe 集**
（`data/probe_set_ids.json`，是没训练过的题目），eval 模式（关 dropout）、
固定 dtype，前向传播一遍，在 decoder 第 4/12/22 层（共 24 层的早/中/晚）
取最后 token 隐状态，算 effective rank、dormant fraction（两个阈值）、
以及附带的谱指标（participation ratio、top-k 方差占比、anisotropy）。

**为什么这么设计**：

- **为什么 probe 集必须冻结**：Q 是拿来跨 checkpoint 比较的。如果每次
  测的题不一样，数值差异就分不清是"模型变了"还是"题变了"。冻结 +
  提交到 git = 任何人可复算。
- **为什么 eval 模式 + 固定 dtype**：dropout 的随机性和数值精度差异都
  会污染谱指标。这些是测量学的基本功——**先保证仪器稳定，才谈得上读数
  有意义**。事实证明这套做到了：两台机器测同一个 ckpt-0，erank 一致到
  小数点后 4 位。
- **为什么选第 4/12/22 层**：早/中/晚各取一层，因为不知道可塑性流失
  会先出现在哪个深度。事后看这个选择有信息量：L4 全程几乎不动，
  L12/L22 有变化——如果只测了一层可能就漏了。
- **为什么还要做 512 vs 2048 的敏感性检查**：谱指标对样本量敏感（文献
  用过 ~10K probe）。我们在 ckpt-0 和 ckpt-200 上用 2048 prompt 复测过
  一次，方向结论一致（L12/L22 下降），PI 问起来接得住。
- **防泄漏规则（proposal §6）**：任何在步骤 t 计算的特征只能用 t 时刻
  可得的信息，不许跨整个 run 做事后归一化。因为最终产品是**实时**预警
  器——实时系统看不到未来，评估时也不许偷看。

### 3.3 Stage B：固定预算 SVAMP 适应（outcome 的测法）

**做法**：从**每一个** checkpoint 出发，跑**一模一样**的适应流程：
256 道冻结的 SVAMP 训练题，50 次 GRPO 更新（配方与 Stage A 相同），
适应前后各在同样 100 道冻结的 SVAMP 评估题上测准确率。
outcome = `svamp_delta` = 适应后准确率 − 适应前准确率。
另外记录第 10/20/30/40/50 步的准确率曲线（适应速度）。

**为什么这么设计**：

- **为什么是 SVAMP**：一个不同于 GSM8K 但同属数学应用题的任务，扮演
  "Stage B 的新任务"。（它和 GSM8K 是否太像，是挂起的待决项——太像的
  话人人都学得会，可能测不出容量差异。这个担忧在结果里得到了印证。）
- **为什么"固定预算"是灵魂**：这直接来自 Madhur 的 framing。比"能力"
  是玄学，比"同样 50 步谁提分多"是科学。50 步预算对所有 checkpoint
  完全相同，题目完全相同，唯一的变量是出发的 checkpoint。
- **为什么每个 checkpoint 跟自己的起点比（difficulty control）**：这是
  Tommy 在 spec 里专门强调的。要排除的混淆是："ckpt-200 学 SVAMP 差，
  也许不是因为可塑性流失，而是它起点就低/题目对它更难"。解决办法：
  (a) 所有 checkpoint 用同一套题；(b) delta 用每个 checkpoint 自己的
  适应前准确率做基准。这样 delta 度量的是"这 50 步预算给你带来了多少
  进步"，与起点高低脱钩。
- **为什么适应阶段也用 GRPO 而不是 SFT**：保持和"Stage B = RL"的项目
  框架一致（apples-to-apples）。这是待决项之一，按推荐默认值实现。

### 3.4 主分析：为什么是 Spearman rho

**做法**：主分析预注册为 rho(erank_L12, svamp_delta)，n=5 个 checkpoint。

- **rho（Spearman 等级相关）只看排名**：把 5 个 checkpoint 分别按 erank
  和 delta 排序，看两个排名是否同序。+1 完全同序，−1 完全反序，0 无关。
  选它而不是 Pearson：不假设线性、对离群值稳健、适合两个量纲完全不同的
  量（一个是几百的秩，一个是 0.0x 的准确率差）。
- **假设预期 rho 为正**：erank 高（可塑性保留好）→ 适应收益大。
- **为什么预注册主分析**：跑之前就写死"主结果只看这一个数"，防止事后
  在几十个指标×层×阈值的组合里挑一个好看的报告（p-hacking）。其他
  相关性都放在 `spearman_table.csv` 里作为探索性结果。
- **n=5 的天然局限**：5 个点的等级相关，|rho| 要到约 0.9 才可能 p<0.05。
  这从一开始就注定 pilot 只能看方向、不能下定论——这是已知且接受的
  局限，不是事故。

### 3.5 防呆装置：sentinel、preflight、manifest

- **sparse_reward_preflight**：训练前先抽样确认奖励信号不是退化的
  （不能所有生成全对或全错——那样 GRPO 的组内对比没有梯度）。这是
  CLAUDE.md 里"notebook 01 的 no-update gate"。
- **update sentinel**：训练中每个窗口验证"权重真的变了"。这个装置是
  Windows v1 事故（见 4.3）之后加的——一次静默 no-op 训练浪费了整轮
  算力，此后所有 run 都带这个哨兵。
- **manifest + 哈希**：每个 run 的 manifest 记录数据集、config 的
  SHA-256。种子重复实验的 manifest 还记录源 checkpoint 的哈希——证明
  "确实是同一批权重，只改了种子"。

---

## 第四部分：三次执行 —— 每一次分别在回答什么问题

关键认知：**三次执行跑的是同一份配方，但每次回答的问题不同。**
另有一次作废的 run（Windows v1）是重要插曲。

### 4.1 实验一：macOS CPU（seed 42）—— "这套流程能跑通吗？信号长什么样？"

- 运行目录：`eaaj-pilot/outputs/local_grpo_gsm8k_eac028bfcc87`
- 环境：MacBook M3 Max，纯 CPU，float32 全程
- **回答的问题**：全流程（4 个 phase）第一次端到端跑通。产出第一组
  完整数据：训练 reward 确实在涨（0.43 → 0.64），erank 确实有变化，
  适应确实能提分。
- **主结果**：rho = **−0.60**（p=0.28，n=5）
- **当时的解读**：方向上似乎支持假设（erank 低 → delta 小？其实符号
  是反的——负 rho 意味着 erank 高反而 delta 小），但 n=5 不显著，
  需要复现验证。**单独看这一次，你无法区分"信号"和"噪声"。**

### 4.2 插曲：Windows v1（作废）—— 免费的一课

- 运行目录：`local_cuda_grpo_gsm8k_6a075c15808e`（保留，标记无效）
- **发生了什么**：第一次 Windows run 把模型参数本身存成 bf16 低精度。
  bf16 只有约 8 位有效二进制小数，而 lr=1e-6 的单步更新量远小于参数
  值的这个精度——**每次更新都被舍入成零**。四个 phase 全部"正常"跑完，
  日志一切体面,但 200 步训练权重最大相对变化只有 2.4e-8：等于没训。
- **教训与遗产**：
  1. 修正方案 v2：fp32 master 权重 + bf16 autocast（计算用低精度省显存,
     权重累积用高精度保住小更新）；
  2. update sentinel 从此成为所有 run 的标配；
  3. v1 数据保留为**阴性对照**——"一个确定没被训练的模型"的 Q 读数,
     反而有参考价值。
- **认知意义**：这次事故本身就是对"dashboard 信号会骗人"这个项目主题
  的意外印证——日志看起来一切正常,模型其实纹丝没动。

### 4.3 实验二：Windows CUDA v2（seed 42）—— "实验一的结果能复现吗？"

- 运行目录：`eaaj-pilot/outputs/local_cuda_grpo_gsm8k_e9b0b52aab6c`
- 环境：RTX 4070 Laptop（8 GiB），fp32 master + bf16 autocast，
  8-bit paged AdamW，gradient checkpointing
- **回答的问题**：同一份配方、同一个种子（42）、不同硬件和数值精度,
  独立从头重跑全流程。如果实验一的 rho=−0.60 是真信号,这里应该重现
  出接近的值。
- **主结果**：rho = **+0.50**（p=0.39，n=5）——**符号反了**
- 33/33 个 sentinel 窗口确认权重在动（v1 的教训落实了）
- **解读**：出现了一个岔路口。两种可能：
  - (a) 硬件/精度差异真的改变了训练动力学（麻烦——意味着结果依赖执行
    环境,科学价值存疑）；
  - (b) 两个 rho 都是噪声,从一个以 0 为中心的分布里随机抽出来的
    （简单——但需要证据）。
  实验二本身无法区分这两种解释。**这正是实验三存在的理由。**
- 附带收获：仪器复现性铁证——两台机器测 ckpt-0 的 erank 一致到小数点
  后 4 位（比如 erank_L12 双方都是 231.7567）。差异出现在训练之后,
  说明差异来自训练过程,不是测量。

### 4.4 实验三：Stage-B 种子重复（seed 43/44）—— "分歧是硬件造成的,还是纯运气？"

- 位置：`…e9b0b52aab6c/adaptation_repeats/seed-43/`、`…/seed-44/`
- **设计的巧妙之处**：**不重训 Stage A**。5 个 checkpoint 原封不动复用
  v2 的（manifest 哈希证明是同一批权重），只把便宜的 Stage-B 适应
  （每个 checkpoint 约 1 小时 GPU）换随机种子重跑。这样硬件、模型、
  题目全部锁死,**唯一的变量是掷骰子的方式**。
- **回答的问题**：svamp_delta 里有多少成分是"这个 checkpoint 的真实
  性质"、多少是"运气"？
- **结果**（seed 43 完成 5/5，seed 44 完成 ckpt-0）：
  - seed 42 的 delta 排名：ckpt-200 > 25 > 0 > 100 > 50
  - seed 43 的 delta 排名：ckpt-50 > 25 > 100 > 200 > 0
  - 两个排名之间的相关 ≈ **−0.50**（近乎反转！）
  - ckpt-0 三个种子的 delta：+0.06 / +0.04 / 0.00
- **解读（三次实验里最决定性的发现）**：同机器、同 checkpoint、同题目,
  只换种子,排名就翻了。**说明 mac/win 的 rho 分歧根本不需要硬件来解释
  ——噪声一个人就能背锅。**由此可知实验一、二的两个 rho 都是噪声量级,
  svamp_delta 这个 outcome 的种子噪声与 checkpoint 间差异同量级。
  旁证：把 seed 42/43 的 delta 取平均后再算 rho,正好 = 0.00。
- 这是三次 run 里最便宜的一次,却提供了信息量最大的一条结论。

### 4.5 三次执行的逻辑链（一句话串起来）

> 实验一发现了一个候选信号（rho=−0.60）；实验二证明它换个环境不复现
> （+0.50）；实验三证明不换环境、光换运气它也不复现（排名翻转）——
> 所以它从头就不是信号,是噪声。**三次执行合起来构成了一次完整的
> "自我证伪"流程,这正是科学方法该有的样子。**

---

## 第五部分：结果全景 —— 数字和它们的含义

### 5.1 核心数字一览

| 观测 | 数值 | 含义 |
|---|---|---|
| ckpt-0 跨平台 erank 一致性 | 小数点后 4 位 | 测量管线可复现（仪器准） |
| 16 次固定预算适应的 delta | 15 正、1 零、0 负 | **没有任何 checkpoint 丧失适应力** |
| dormant fraction（全部测量） | 全为 0.0 | 副指标无信号（0.5B 规模看不到躺平神经元） |
| erank L12/L22 @ ckpt 25–100 | 低于 ckpt-0 数个百分点 | 主指标有小幅真实变动,200 时部分回升 |
| erank L4（全程） | 变化 <3% | 早层几乎不动 |
| ckpt-0→200 权重最大相对变化 | 6.06e-6（v2） | Stage A 对模型的扰动极小 |
| 主 rho（mac / win / seed43） | −0.60 / +0.50 / −0.50 | 方向不稳定,均不显著 |
| rho（seed 42/43 delta 均值） | 0.00 | 平均掉运气后,相关消失 |
| seed 42 vs 43 delta 排名相关 | ≈ −0.50 | outcome 被种子噪声主导 |

### 5.2 为什么结果长这样：三层原因

**第一层：outcome 的噪声地板太高。**
100 道评估题的二项噪声：单次准确率的标准误 ≈ √(0.5×0.5/100) ≈ ±5 个
百分点。而 checkpoint 之间的 delta 差异总共才 0.02–0.13。**要分辨的信号
和量尺自带的抖动是同一个量级。**实验三只是把这个纸面推算变成了实测。

**第二层：pilot 剂量下,"病"根本没发生。**
lr 1e-6 × 200 步,权重总共动了百万分之六。16 次适应全部非负,dormant
恒为 0。想预警的现象（可塑性流失导致适应力下降）在这批对象身上不存在。
**在一群健康人身上,无法检验"血检指标能否预测生病"。**

**第三层：n=5 让 rho 天生脆弱。**
5 个点的等级相关,换一两个位次就能从 −0.6 跳到 +0.5。mac 和 win 的
"矛盾"其实不矛盾——都是从以 0 为中心的噪声分布里抽的样本。

### 5.3 目前的准确结论（三档表述）

**铁的事实（产物直接支撑）：**
1. 测量管线可复现（跨平台 4 位小数一致）。
2. 本 pilot 配方下未观察到任何固定预算适应力下降（15 正 1 零 0 负）。
3. dormant fraction 无信号；erank 有小幅真实变动。
4. 主相关方向不稳定且均不显著；outcome 被种子噪声主导。

**稳的推论（可以讲,注明是推论）：**
- RQ1 目前**未被回答**——既没被支持也没被否定。原因：自变量（可塑性
  损伤）没造出来 + 因变量（delta）太吵。
- 当前 pilot 设计的灵敏度不足以检验 RQ1；正式实验必须修改设计。

**绝不能说的话：**
- ❌ "Q 能/不能预测适应力"（两个方向都没证据）
- ❌ "RLVR 损害学习能力"（违反 framing 红线,且数据里毫无迹象）
- ❌ 引用任何单个 rho 当结论（已被实验三证明是噪声量级）
- ⚠️ 预注册的三种子汇总分析要等 seed 44 跑完才能做,目前一切跨种子
  数字都只是描述性的。

### 5.4 对正式实验的三条设计修正（pilot 的净产出）

1. **把 Stage A 加狠**：更多步数（200 → 500+）、更高 lr、训到 reward
   饱和后继续压、或上 1.5B 模型——总之要真的造成可测的可塑性损耗。
   顺带跑一条 KL β>0 的对照（Madhur 点名要的 baseline）。
2. **把 outcome 噪声压下去**：评估集 100 → 300+ 题；每个 checkpoint
   至少 3 个适应种子取平均（实验三已验证此流程可行）；checkpoint 数量
   加密（n=5 太少）。
3. **重新考虑任务家族**：15/16 全正的一种解读是 SVAMP 离 GSM8K 太近,
   人人都学得会。考虑换/加一个更远的任务（如 ProntoQA）。

---

## 第六部分：总体评价 —— 这轮 pilot 做得怎么样

### 6.1 记分卡

| 维度 | 得分 | 理由 |
|---|:---:|---|
| 执行严谨度 | 9/10 | 预注册零改动、冻结数据、哈希 manifest、sentinel、阴性对照保留、compute log 全程 |
| 可复现性 | 9/10 | 跨平台 4 位小数一致,少有人能拿出的硬证据 |
| 成本效率 | 10/10 | 全本地硬件,300 Colab 计算单元分文未动 |
| 方法论信息量 | 8/10 | 实验三用最便宜的 run 挖出全项目最关键的情报（噪声 ≥ 信号） |
| 对 RQ1 的证据贡献 | 2/10 | 假设未被检验成——不怪执行,但事实如此 |
| 设计前瞻性 | 5/10 | 100 题的 ±5pp 噪声和 lr 1e-6 的温和性,纸面上本可预估 |

**作为 pilot：8/10。** pilot 的三大天职（跑通管线、摸清噪声、给正式
实验定参数）全部完成,零预算消耗。扣分在于部分坑本可纸面预见。

**作为 RQ1 的证据：2/10。** 假设仍是白纸。汇报时两个分数都要亮,
不能拿执行分粉饰证据分。

### 6.2 直观比喻收尾

你们要造一台地震预警仪（Q 指标）。这三次实验相当于：

1. 仪器组装好了,两个实验室校准读数完全一致（实验一、二的测量部分）；
2. 等了三个月,一次地震都没来（Stage A 太温和,"病"没发生）,所以
   "预警灵不灵"没测成；
3. 但等待期间发现了更要紧的事：**记录仪自身的抖动比小地震的信号还大**
   （实验三）。不换记录纸（加大评估集、多种子平均）,就算地震来了也
   测不出来。

钱花得值:**用最便宜的方式买到了"下一次该怎么做"的完整答案。**
没买到的是假设本身的答案——那本来就不该指望 pilot 给。

### 6.3 下一步（按预注册计划）

1. 跑完 seed 44 剩余 4 个 checkpoint（200/25/50/100 顺序）；
2. 生成预注册的 seeds-42/43/44 汇总分析（哪怕预期仍是 null,走完
   预注册流程本身就是可信度）；
3. 把第 5.4 节的三条设计修正整理成提案发给 Tommy/Madhur；
4. 四个挂起的待决项交团队拍板：base vs Instruct、适应用 GRPO vs SFT、
   SVAMP 是否太近、KL β>0 对照。

---

## 附录：产物索引（每个数字都能落到文件）

| 内容 | 路径 |
|---|---|
| 预注册配方 | `eaaj-pilot/pilot_config.json` |
| 实验一（mac）产物 | `eaaj-pilot/outputs/local_grpo_gsm8k_eac028bfcc87/` |
| 实验二（win v2）产物 | `eaaj-pilot/outputs/local_cuda_grpo_gsm8k_e9b0b52aab6c/` |
| 实验三产物 | `…e9b0b52aab6c/adaptation_repeats/seed-43/`、`…/seed-44/` |
| v1 作废分析 | `eaaj-pilot-win4070/WIN4070_RUN_ANALYSIS.md` |
| 三实验合并报告 | `PILOT_COMBINED_REPORT_3EXPERIMENTS_ZH.md`（及英文版） |
| 给队友的证据包 | `experiment 1/pilot_evidence_pack/`（含 README 导览） |
| 计算账本 | `eaaj-pilot/compute_log.md` |
| 项目简报（英文） | `agent_doc/AGENT_BRIEFING_first_experiment.md` |
