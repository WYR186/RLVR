# Experiment 1 各版本从零讲解（零基础完全版）

日期：2026-07-18。写给零 ML 背景的读者，也写给未来需要快速回忆全貌的自己。
所有数字都来自已提交的产物（run 目录、evidence pack、`compute_log.md`），
并与《PILOT_COMBINED_REPORT_3EXPERIMENTS.md》逐一核对过。凡是"事后算的、
没预注册的"数字，文中都标注了"描述性"。

**这份文档和已有文档的分工：**

| 你想要什么                                       | 去看哪份                                         |
| ------------------------------------------- | -------------------------------------------- |
| 纯事实数字、可引用的报告                                | `PILOT_COMBINED_REPORT_3EXPERIMENTS(_ZH).md` |
| Mac vs Windows 差异的深挖                        | `PILOT_TWO_RUNS_EXPLAINED_ZH.md`             |
| 面向队友的讲解（默认有些 ML 基础）                         | `PILOT_UNDERSTANDING_GUIDE_ZH.md`            |
| **从零开始、连名词一起补、把所有版本（含作废的、没采用的、还没跑的）串成一个故事** | **本文**                                       |

---

## 第一部分：名词课 —— 读懂后文需要的全部概念

每个概念给一个比喻 + 一句严格定义。已经懂的可以跳过对应条目。

### 1.1 语言模型、参数、权重

**比喻**：模型是一台巨大的"下一个词预测机"，内部有几亿个可调的旋钮。
**定义**：Qwen2.5-0.5B 是阿里开源的语言模型，含约 5 亿个参数（parameter，
也叫权重 weight）——每个参数就是一个实数。训练 = 微调这些数，让模型的
输出变好。我们全程用同一个精确版本（revision `060db649…`），保证所有
实验面对的是同一台"出厂机器"。

### 1.2 训练、学习率、update、checkpoint

**比喻**：训练像下山找最低点，学习率（learning rate, lr）是每一步迈多大。
**定义**：一次 **update**（更新）= 根据一批数据算出每个参数该往哪调，
然后把所有参数移动一小步，步长由 lr 控制。我们的 lr = 1e-6（0.000001），
是非常小的步子——这个数字后面会成为一场事故的主角。
**Checkpoint** = 训练到某一步时把全部参数存档。存档之间互不影响，
可以从任何一个存档出发继续做别的事——这正是本实验的核心操作。

### 1.3 RLVR 和 GRPO：让模型做题、按对错给分

**比喻**：让学生做数学题，答对给 1 分、答错 0 分，然后根据得分调整
学习方法。不告诉他标准解法，只告诉他对没对。
**定义**：RLVR（Reinforcement Learning with Verifiable Rewards）是用
"可机器验证的奖励"做强化学习。**GRPO** 是具体算法：对同一道题让模型
生成 **8 个答案**（带随机性，温度 0.7），给每个答案打分（答案数字与
标准答案完全一致 = 1，否则 = 0），然后**在这 8 个答案内部比较**——
比组内平均好的答案，其写法被加强；比平均差的被削弱。
关键推论：**如果 8 个答案得分全一样（全对或全错），组内没有"好坏之差"，
这道题这一轮就学不到任何东西。**后文的 preflight 检查就是在防这个。

### 1.4 On-policy：为什么"同一配方跑两次结果不同"是正常的

GRPO 是 **on-policy**（在线策略）算法：模型用**自己刚生成的答案**更新
自己。生成带随机性 → 第一步生成的答案就有随机差异 → 更新后的模型有
微小不同 → 下一步生成的答案更不同……200 步滚下来，两条轨迹就是两个
不同的模型。**这不是 bug，是这类算法的本性**，相当于"同一个班级平行
开两个班，教学大纲一样，但每堂课的课堂讨论不一样"。读第五部分的
版本对比时，请始终带着这个心智模型。

### 1.5 随机种子（seed）

**比喻**：发牌前洗牌的固定手法。同一副牌、同一手法，发出来的牌一样。
**定义**：程序里的"随机"其实由种子决定。固定 seed=42 让单机重跑可复现；
但注意 1.4——**跨机器时即使 seed 相同，浮点运算顺序不同也会让随机采样
从第一步就分岔**。换机器 ≈ 换了一次种子。

### 1.6 浮点精度 fp32 / bf16：本故事最大事故的根源

计算机存实数是"科学计数法 + 有限位数"。

- **fp32**（32 位浮点）：约 7 位有效数字。
- **bf16**（16 位浮点）：只有约 **2–3 位有效数字**，但省一半内存，
  GPU 上更快，是深度学习常用的省显存手段。

**事故机制（大白话）**：假设某个参数值是 0.9，一次更新想给它加
0.000000009（lr=1e-6 乘上梯度，相对变化 ~1e-8）。用 fp32 记账，
0.900000009 存得下；用 bf16 记账，只有 3 位有效数字，0.9 + 0.000000009
四舍五入后**还是 0.9**。每一步更新都被舍入吃掉——模型"训练"了 200 步，
参数一动没动。这就是后文 Windows v1 版本发生的事。
**正确做法（v2 采用）**：参数本体用 fp32 保存（"master weights"，
总账用高精度记），只在前向计算时临时转 bf16 加速（autocast）。

### 1.7 GSM8K 和 SVAMP：两套小学数学应用题

- **GSM8K**：小学数学应用题数据集（"James 每周写 42 封信……一年写多少"
  这种）。我们冻结了其中 512 道做训练、64 道做考试。
- **SVAMP**：另一套更短的小学数学应用题。冻结 256 道做"新任务训练"、
  100 道做"新任务考试"。
  它们的角色：GSM8K 是"第一学期课程"（Stage A），SVAMP 是"第二学期新
  课程"（Stage B）。一个已被数据实锤的隐患：这两套题**太像了**（都是
  文字数学题），第一学期学的可以直接搬到第二学期用——详见 5.4 节。

### 1.8 两个"体检指标"（Q）

给模型喂 512 个固定问题（probe 集），在第 4、12、22 层（共 24 层，代表
浅/中/深）各截取内部激活向量，对每层算两个数：

- **Effective rank（有效秩，主指标）**：把 512 个激活向量堆成矩阵做
  SVD（奇异值分解——把数据的"伸展方向"按重要性排序），算谱熵的指数
  `erank = exp(−Σ p_i log p_i)`。**直觉：模型内部表征还"张开"多少个
  有效维度。**一个装满弹簧的床垫，erank 高 = 每根弹簧独立支撑，
  erank 低 = 弹簧都被压连成一片，只剩几个方向能动。假说：维度塌缩 =
  表征变僵 = 学新东西的余地变小。
- **Dormant-neuron fraction（休眠神经元比例，副指标）**：MLP 层里
  平均激活幅度低于阈值（τ=0.025 或 0.1）的神经元占比。**直觉：多少
  神经元已经"躺平"完全不参与计算。**

测量的三道锁（保证跨存档、跨机器可比）：固定 probe 集（永远同 512 题）、
eval 模式（关闭训练期的随机性）、固定 dtype（fp32）。后文会看到这三道
锁效果好到什么程度（跨机器一致到小数点后 4 位）。

### 1.9 固定预算适应（fixed-budget adaptation）：本项目的"体能测试"

**比喻**：不问"这个学生素质如何"（说不清），只问"给每个学生完全相同的
50 课时学新科目，谁进步大"（可测量）。
**定义**：从每个 checkpoint 出发，用**一模一样的预算**（50 步 GRPO、
同 256 道 SVAMP 训练题）学习新任务，前后各考一次同一套题（100 道），
**outcome = 考后准确率 − 考前准确率（delta）**。每个 checkpoint 和
"自己的考前分"比，所以起点高低不构成不公平（但见 5.2 节的天花板效应
——起点太高会没有上涨空间，这是另一种污染）。
这个设计直接来自 mentor（Madhur）的红线：**不许声称"RLVR 降低了模型的
学习能力"**（不可测的大词），只许测"固定预算下对指定任务家族的适应量"
（任务、预算、基线全部事先定死的量）。

### 1.10 Spearman 相关、p 值、以及 n=5 意味着什么

- **Spearman rho**：两组数的**排名**一致程度，+1 = 排名完全一致，
  −1 = 完全颠倒，0 = 无关。我们的主分析：5 个 checkpoint 按 erank 排名
  vs 按 SVAMP delta 排名，是否一致？
- **p 值**：假如真相是"毫无关系"，纯靠运气得到这么极端数字的概率。
  惯例 p < 0.05 才叫"显著"。
- **n=5 的残酷事实**：只有 5 个点时，随便换动一两个位次 rho 就能从
  −0.6 跳到 +0.5；要显著需要 |rho| ≥ 0.9。**这个先天弱点贯穿全部
  版本的结果解读。**

### 1.11 本项目的工程黑话小抄

| 词                 | 意思                                       |
| ----------------- | ---------------------------------------- |
| Stage A / Phase 1 | GSM8K 上的 GRPO 训练（200 步，存 5 个 checkpoint） |
| Phase 2           | 每个 checkpoint 测 Q                        |
| Stage B / Phase 3 | 每个 checkpoint 做固定预算 SVAMP 适应             |
| Phase 4           | 统计分析（Spearman）                           |
| dashboard         | 训练时每步记录的常规信号（reward、熵、梯度范数等）             |
| preflight         | 花钱前的检查（见 1.3 末尾）                         |
| sentinel          | 哨兵：每 25 步测"参数真的动了吗"（v1 事故后加装）            |
| stratum（执行层）      | 同一配方在某台机器/精度组合下的一次完整执行                   |
| manifest          | 记录环境、版本、数据哈希的清单文件，保证可审计                  |

---

## 第二部分：Proposal 想证明什么

### 2.1 要解决的真实痛点

训练推理大模型常分多阶段 RL：先任务 A 练一轮，再任务 B 练一轮。一个
昂贵的失败模式是 **Stage B 停滞（stall）**：A 阶段一切正常，到 B 阶段
突然学不动——发现时 B 的算力已经烧完。对前沿大模型，这是几万到几十万
美元级的浪费。

### 2.2 团队的赌注（proposal "Seeing the Stall Coming"）

> Stage A 的训练会让模型**悄悄失去可塑性**（学新东西的内在能力）。
> 这种流失在 dashboard 信号上看不出来，但在模型内部激活的几何结构里
> 留有痕迹（Q）。读出 Q，就能在 Stage A 进行中**提前**预警"Stage B
> 会停滞"，且比 dashboard 信号早。

**体检比喻**（贯穿全项目）：病 = 可塑性流失；病发作 = Stage B 停滞；
血检指标 = Q；体能测试 = 固定预算适应；现有听诊器 = dashboard 信号。
我们在造一台"比听诊器更早发现病"的血检仪。

### 2.3 RQ1（experiment 1 服务的问题）

**Stage A 各 checkpoint 的 Q 值，与该 checkpoint 的固定预算 SVAMP
适应量（delta），是否相关？** 这是整个项目的第一块砖：如果 Q 连
"事后同时测"都不相关，"提前预警"就无从谈起。

### 2.4 Aaron（Person 4）的分工

早期预警诊断：Q 指标的代码实现、dashboard 信号记录、每 checkpoint 的
测量产物。Experiment 1 的全部本地执行、分析和文档都在这个职责内。

---

## 第三部分：实验 1 的设计蓝图（每个数字为什么是这样）

一句话版设计：**GRPO 训练 Qwen2.5-0.5B 做 512 道 GSM8K（对/错给分），
在 0/25/50/100/200 步各存档；每个存档测 Q；再从每个存档出发用相同的
50 步预算学 SVAMP；看 Q 能否预测各存档的适应量。**

| 设计选择                                        | 为什么                                                                   |
| ------------------------------------------- | --------------------------------------------------------------------- |
| 0.5B 小模型                                    | pilot 的目的是验流程不是出论文数字；小模型本地笔记本能跑，零成本试错（proposal 设想的正式"危险区"是 1.5B 全参微调） |
| 512 题、200 步、lr 1e-6、KL β=0                  | Tommy 的 spec 原样执行；β=0 = 不加"别离出厂状态太远"的约束，理论上更容易造成可塑性损伤                 |
| 存档点 0/25/50/100/200                         | 0 = 出厂对照（没训过的基线）；前密后疏，因为文献提示谱变化多发生在训练早期                               |
| Q 测三层（4/12/22）                              | 浅/中/深各取一层；预注册主指标 = 第 12 层 erank                                       |
| SVAMP 适应"人人 50 步"                           | 固定预算是 Madhur 红线的操作化（见 1.9）                                            |
| 主分析 Spearman n=5                            | 事先定死、写进代码，防止事后挑好看的指标（p-hacking）                                       |
| 全套防呆（preflight/sentinel/manifest/validator） | 见各版本的实战故事——每一件都在 pilot 里派上了用场                                         |

**防呆装置实测记录**（这些不是摆设，都触发过或核验过）：

- sparse-reward preflight：Mac 8 组里 7 组奖励有方差、Windows 8/8——
  都有学习信号，放行（若 8 组全恒定则停下上报，不许静默换 reward）。
- update sentinel：每 25 步验证参数真的在动，v2 起装备，33/33 窗口通过。
- validator（Stage-B 重复跑的验收器）：50 步全完成 + 完整考试曲线 +
  哨兵全过 + 基线核对 + 数值有限 + telemetry 齐 + 无安全停，全过才算数。

---

## 第四部分：版本总表

"experiment 1" 其实是**一个设计蓝图 + 七次不同性质的执行/衍生**。
先一张表看全，第五部分逐个讲透。

| #   | 版本                    | 时间       | 目录/位置                                        | 状态                           | 一句话定位                 |
| --- | --------------------- | -------- | -------------------------------------------- | ---------------------------- | --------------------- |
| 0   | Phase-0 开发验证          | 07-07    | `eaaj-pilot/tests/`、`scripts/`               | 通过                           | 证明代码算得对（不是实验）         |
| 1   | **Mac CPU run（实验一）**  | 07-07 起  | `outputs/local_grpo_gsm8k_eac028bfcc87`      | 完成                           | 第一次完整执行：流程能跑通吗？       |
| A   | MPS 插曲（未采用）           | 07-08    | `LOCAL_EXPERIMENT_PLAN.md` §07-08            | 放弃                           | 试过的加速路线，实测不快          |
| 2   | **Windows v1（作废）**    | 07-09    | `outputs/local_cuda_grpo_gsm8k_6a075c15808e` | 无效，留作负对照                     | bf16 精度事故：跑完了但什么都没训   |
| 3   | **Windows v2（实验二）**   | 07-09 起  | `outputs/local_cuda_grpo_gsm8k_e9b0b52aab6c` | 完成                           | 修复后的重跑：实验一能复现吗？       |
| 4   | **Stage-B 种子重复（实验三）** | 07-12 起  | `…e9b0b52aab6c/adaptation_repeats/`          | seed 43 完成 5/5；seed 44 仅 1/5 | 只换适应种子：分歧是硬件还是运气？     |
| 5   | **Experiment 1.5**    | 07-16 冻结 | `experiment 1.5/`                            | 计划冻结，仅跑过 CPU smoke，正式未开跑     | 加大剂量 + 压低噪声：先把"病人"造出来 |
| B   | Evidence pack         | 07-15    | `experiment 1/`（含 zip，已发 Slack）              | 已交付                          | 全部证据的打包快照，供团队审计       |

---

## 第五部分：逐版本详解

### 版本 0：Phase-0 开发验证（07-07，Mac，15 秒）

**是什么**：37 个单元/契约测试 + 1 步迷你 GRPO 冒烟测试 + 8 题 Q 测量
干跑。全部在 Mac CPU 上、模型缓存热身后 15 秒内跑完。
**为什么存在**：金属探测器过一遍再上跑道。metric 公式对不对、reward
解析对不对、GRPO 的 API 契约对不对——这些错误如果混进正式跑，产出的
是"看起来像结果的垃圾"。
**意味着什么**：代码正确性有底。注意 `outputs/dry_run_metrics.json`
是管线测试产物，**永远不是科学结果**（CLAUDE.md 明文规定）。

### 版本 1：Mac CPU run —— 实验一（07-07 起，`eac028bfcc87`）

**是什么**：设计蓝图的第一次完整执行。MacBook M3 Max，纯 CPU、全程
fp32、标准 AdamW。seed 42。
**过程**：Phase 1 第一天跑到第 67 步（5.8 小时，约 300 秒/步）被中断，
从 trainer 存档续跑完成；后续 Phase 2/3/4 依次完成。
**结果**（全部已提交）：

- 训练 reward 分段均值 0.430 → 0.640：**真的在学**。
- GSM8K 考试（64 题）：0.4375 → 最高 0.5469：学的东西泛化到了考试。
- erank：第 22 层早期塌 9–13% 且不恢复（ckpt-200 仍 −11.1%）；
  第 12 层先塌后**回弹到 +0.7%**；第 4 层基本不动。
- dormant fraction：全程全层全阈值 = 0。
- SVAMP 适应 delta（ckpt 0/25/50/100/200）：+5/+13/+8/+2/+4（百分点），
  全为正。
- 预注册主分析：**rho(erank_L12, delta) = −0.60，p = 0.285**（不显著）。
- 一个重要副现象：**未适应就考 SVAMP 的起点分从 0.53 一路涨到 0.67**
  ——GSM8K 学的直接搬到了 SVAMP（两套题太像），伏笔见 5.2。

**意味着什么**：

1. 流程端到端可行，仪表读数"长得像话"。
2. 但单看这一条 run，你可能会讲出"erank 越低适应越好？！训练越多
   适应越差？"的故事——**别急，等版本 3 揭晓这个故事的成色**。

### 插曲 A：MPS 调查 —— 试过但没采用的版本（07-08，1.5 小时）

**是什么**：Mac 的 GPU 加速接口叫 MPS。CPU 每步 300 秒太慢（中断在
第 67 步就是因为太慢），于是在切换前**先在真实工作负载上做了基准测试**。
**发现**：裸算力测试很漂亮，但真实 GRPO 更新实测 265–320 秒/步——
与 CPU 持平。根因：transformers 的逐 token 生成在 MPS 上每个 token
都要同步一次，开销吃掉全部加速。结论：不采用（修补代码保留在
`src/mps_compat.py`）。
**意味着什么**：一次教科书式的"先测再切"。如果凭裸基准直接切过去，
会白花一天迁移然后发现不快。也正是这次失望催生了"去 Windows 4070 上
跑 CUDA 层"的决定——从而引出版本 2 的事故。

### 版本 2：Windows v1 —— 作废的版本（07-09，`6a075c15808e`，负对照）

**是什么**：同一配方在 Windows RTX 4070 Laptop（8 GiB 显存）上的第一次
执行。为省显存，参数本体直接用 **bf16** 存（伏笔回收：见 1.6）。
**过程**：工程上完美——四个 phase 端到端 7.3 小时跑完（Phase 1 仅
3.09 小时，比 fp32 快），所有产物、遥测齐全，dashboard 看着完全正常。
**然后发现**：事后对比权重存档，ckpt-0 → ckpt-200 所有参数组的最大
相对变化只有 **2.4e-8** ——即亿分之二点四。对照组：Mac 同指标 3.9e-6，
且 reward 实打实从 0.36 学到 0.65。**200 步"训练"没有改变任何参数**：
lr=1e-6 的每一步更新都小于 bf16 的 3 位有效数字能记录的最小刻度，
全部被四舍五入吃掉（1.6 节的机制原文重演）。
连带的：v1 的 reward 全程平（0.366→0.380，纯抖动）、erank 各层变化
≤0.14%、SVAMP delta 在 ±2pp 内游走、主相关 rho = −0.051（p=0.935）——
**全部是"什么都没发生"的读数，而不是"没有关系"的证据**。
**处置**：整条 run 标记无效，保留为**负对照**（"未训练的模型长这样"
的参照系）；诊断报告 `WIN4070_RUN_ANALYSIS.md`。

**意味着什么（这是全 pilot 最值钱的一课）**：

1. **存在一种失败：仪表盘完全正常，但实验根本没发生。**如果这发生在
   花钱的正式实验里，我们会得出"Q 与适应性无关"的假阴性结论，且不知道
   自己错了。
2. 由此诞生两件装备：**update sentinel**（每 25 步实测参数动没动）和
   **step-25 人工 stop/go 关卡**（哨兵首个窗口不过 = 立即 Ctrl+C）。
3. 预注册的 Colab 笔记本配方里也有同样的 bf16 隐患——**这个 bug 在
   花任何预算之前被本地跑批抓住了**，单这一条 pilot 就回本。

### 版本 3：Windows v2 —— 实验二（07-09 起，`e9b0b52aab6c`）

**是什么**：修复精度后的正式重跑。方案：**fp32 master 权重**（总账
高精度）+ bf16 autocast（计算时临时降精度加速）+ 8-bit 分页 AdamW
（把优化器状态压进 8 GiB 显存）+ 梯度检查点。seed 42。micro-batch
4 × 梯度累积 16（与 Mac 的 8×8 等效，均为每次更新 64 个答案）。
**过程（诚实记录的执行事故，全部在案）**：

- Phase 1 在约 25/50/75 步三次中断并修复——恢复了模型权重和调度器
  位置，但优化器动量与随机数状态无法完整恢复（bitsandbytes 在 Windows
  上存优化器状态会挂，被迫 `save_only_model=True`）。轨迹有效，但
  **不等价于一条不间断的 run**。
- Phase 3 的 ckpt-50 遇到一次瞬时显存溢出（OOM）：失败产物隔离保存，
  该点用原配方在新进程从头重跑。
- 显存预检的"全几何探针"报出 10.809 GiB 峰值预留——超过 8 GiB 物理
  显存本身，判定为 Windows WDDM/分配器的统计口径问题，记为 deviation。
- **哨兵 33/33 窗口全过**；ckpt-0→200 最大相对权重变化 6.06e-6——
  这次训练是真的。
  **结果**：
- 训练 reward 0.354 → 0.582：在学，但全程比 Mac 低 0.06–0.11。
- GSM8K 考试 0.3594 → 0.42–0.47 区间抖动（64 题的单次评测噪声本身
  就有 ±6pp 量级；注意 ckpt-0 两台机器就差了 8pp——同一个模型！
  因为考试也是带温度采样的生成）。
- erank：ckpt-0 与 Mac **一致到小数点后 4 位**（231.7567）——测量
  三道锁全部生效；第 22 层塌 8–10% 不恢复（与 Mac 一致）；但第 12 层
  塌到 −7.4% **不回弹**（Mac 回弹了——中层"塌后是否回弹"是轨迹
  相关的）；2048 题加大 probe 复核方向一致（排除小 probe 假象）。
- dormant fraction：仍全程为 0（与 Mac 一致）。
- SVAMP delta：+6/+11/+3/+5/+12，全为正；起点分几乎不动（0.51–0.56，
  没有 Mac 那种迁移暴涨）。
- 预注册主分析：**rho = +0.50，p = 0.391**（不显著）。

**意味着什么**：

1. **现象层高度复现**：训练真实、深层谱收缩、适应全为正、dormant 全 0
   ——两台机器都看到。谱收缩由此成为 pilot 最扎实的科学观察。
2. **统计层完全翻转**：主相关从 −0.60 变 +0.50。同设计同 seed，只换
   执行环境，结论的"方向"就反了。只跑其中任何一条都会编出一个方向
   相反但听起来都头头是道的故事——两条一起看才知道都是给噪声编剧情。
3. 但此时还有一个悬念：翻转是"硬件/精度差异"造成的，还是纯随机？
   这就是版本 4 要回答的。

### 版本 4：Stage-B 种子重复 —— 实验三（07-12 起，seed 43 完成、44 未完）

**是什么**：**固定住 Windows v2 的五个 checkpoint 一个字节不动**，只把
Stage B（SVAMP 适应）的随机种子从 42 换成 43、再换 44，重做适应。
这是一次干净的对照实验：种子是唯一变量。
**为什么这样设计**：如果只换种子（同机器、同存档、同精度）就能让结果
大变，那么版本 1 vs 版本 3 的分歧就不需要"硬件问题"来解释——纯随机
就够了。反之若种子间稳定，才值得去查硬件。
**过程**：每次重跑通过全套 validator 验收（见第三部分）；有一次
wrapper 因 PowerShell 把警告流当错误而失败——失败证据保留、新进程
原配方重跑（第一性原则：任何失败不静默重试，先留证）。
**结果**：

- seed 43（5/5 完成）：delta = +4/+8/+9/+6/+5，仍全为正。
- 但 checkpoint **排名近乎反转**：seed 42 的排名是 200 > 25 > 0 > 100
  
  > 50，seed 43 是 50 > 25 > 100 > 200 > 0；两个排名的相关 ≈ **−0.50**。
- 描述性地把 seed 43 的 delta 代入主分析：rho = **−0.50**——同一台
  机器、同五个存档，只换种子，主相关就从 +0.50 翻到 −0.50，**翻转
  幅度与"换一台机器"一模一样**。
- seed 44：只完成 ckpt-0（delta = 0.00，16 次适应中唯一的零，也是
  三个种子下 ckpt-0 的第三个读数：+6/+4/0，实测种子噪声 SD≈0.031）。
  其余 4 个点未跑，**预注册的三种子汇总分析被门控在 seed 44 完成之后**。

**意味着什么（pilot 统计教训的定音锤）**：

1. 版本 1 vs 3 的相关翻转**不需要也不应该用"某台机器有问题"解释**：
   换个种子就能造出同样大小的翻转。n=5、单种子的 Spearman 就是噪声
   主导的读数。
2. 这直接产出正式实验的硬设计要求：**多个独立 Stage-A 种子 × 每
   checkpoint 多个适应种子，缺一不可**。
3. 注意诚实边界：三个种子共享同一条 Stage-A 轨迹（都从 v2 的存档出发），
   所以它们检验的是 Stage-B 噪声，**不构成独立的 Stage-A 样本**。

### 版本 5：Experiment 1.5 —— 已冻结待跑的下一步（07-16 冻结计划）

**是什么**：介于 pilot 和正式实验之间的本地实验。诊断是：pilot 证明了
仪器可靠，但**病人没生病**——lr 1e-6 × 200 步太温和，没造成任何可测的
可塑性损伤（16 次适应 15 正 1 零；dormant 恒 0；权重总位移仅 6e-6 量级），
同时 **outcome 的噪声（±5pp）和信号（2–13pp）同量级**。仪器再准，
病人健康 + 尺子抖，什么也测不出。
**只动三组旋钮，其余逐字节冻结**（模型、数据、probe、Stage-B 配方、
v2 执行剖面、全套防呆原样保留）：

1. **剂量加大**：lr 1e-6 → 1e-5（10 倍），步数 200 → 500（前 5 个
   存档点与 pilot 网格相同，可跨实验对齐，另加 300/400/500）。
2. **噪声压低**：评估集 100 → 300 题（单次误差 5.0pp → 2.9pp）；
   每个 checkpoint 预注册 3 个适应种子，主 outcome = 三种子均值
   （均值差的误差 ≈ 2.5pp）。
3. **样本加密**：Q 测 8 个 checkpoint，适应做其中 6 个（n: 5→6）。
   **预注册的裁决规则（跑之前定死）**：先过两道"实验条件成立吗"的门——
   MC1：500 步后 erank_L12 位移是否 ≥10%（Q 真的动了）；MC2：是否存在
   后期 checkpoint 的三种子平均 delta 比 ckpt-0 低 ≥0.05（适应力真的
   降了）。两门的通过/失败组成 2×2 矩阵，每格的解释都已写死——包括
   "MC1 过而 MC2 不过 = SVAMP 太近的正面证据"和"MC2 过而 MC1 不过 =
   **反对 erank 作为预警指标的有效证据**"。这个设计的意义：**无论结果
   落在哪格，实验都不白跑。**
   **执行计划**：Windows 4070，约 34–35 GPU 小时分 4–6 晚；checkpoint
   顺序 endpoint-first（0 → 500 → 200 → …），跑完前两个点就能提前读出
   "两端点适应力差"这个最关键信息；lr 加大后若训练崩溃（熵坍缩等），
   安全停 + 保留诊断——**坍缩本身是观察对象，不是故障**。零 Colab 消耗，
   预算原封留给正式实验。
   **当前状态（2026-07-18）**：计划与配方已冻结；只跑过 Mac 上的 CPU
   smoke 验证（`smoke_outputs/exp15_cpu_grpo_gsm8k_60d351e61c07`）；
   **正式 CUDA run 尚未开始**（`outputs/` 下无 `exp15_cuda_` 目录）。

**意味着什么**：pilot 的三大教训（剂量不够、噪声太大、单种子不可信）
被逐条转译成了预注册的设计参数；exp 1.5 的结果将直接决定正式实验
（experiment 2）的最小剂量、种子数/评估集大小、以及是否必须更换
Stage-B 任务家族。

### 附：Evidence pack（`experiment 1/` 目录）

**是什么**：不是一次实验，是**证据打包**——把 Mac run 和 Windows v2
的全部分析产物（config、manifest、preflight、dashboard、每 checkpoint
的 Q 测量、适应 summary、分析表、图）复制成一个自包含目录并压缩
（`exp1_result_aaron.zip`），已发到团队 Slack。
**意味着什么**：任何队友不需要访问两台机器就能核查每一个数字。本文
所有数字也都能在这个包里找到。

---

## 第六部分：把所有版本串成一页故事

> 07-07：先用 15 秒的测试确认代码算得对（版本 0），随即在 Mac 上开跑
> 第一次完整实验，因 CPU 太慢（300 秒/步）中断在第 67 步。
> 07-08：试图用 Mac 的 GPU（MPS）加速，实测与 CPU 同速，放弃（插曲 A），
> 转而决定动用 Windows 游戏本的 4070。
> 07-09：Windows 首跑 7.3 小时完美收工——然后发现它是一场空：bf16
> 精度把每一步更新四舍五入吃掉了，200 步没改一个参数（版本 2，作废，
> 留作负对照；由此发明哨兵）。fp32-master 方案重跑成功（版本 3）。
> 与此同时 Mac 那条也续跑完成（版本 1）。
> 对账时刻：两台机器现象层高度一致（真的在学、深层谱收缩 8–13% 不
> 恢复、适应全为正、dormant 恒 0），但预注册主相关一边 −0.60 一边
> +0.50，方向相反。
> 07-12 起：为判定翻转是硬件还是运气，固定 Windows 存档只换适应种子
> 重跑（版本 4）——种子 42→43 就让相关从 +0.50 翻到 −0.50。结论：
> 是运气。n=5 单种子的相关系数本身就是噪声。
> 07-14/15：把三次执行写成 facts-only 综合报告 + 理解指南，证据打包
> 发团队（附录 B）。
> 07-16：把全部教训转译成预注册的 experiment 1.5（版本 5）：剂量
> ×10、步数 ×2.5、评估集 ×3、种子 ×3、裁决规则 2×2 事先写死——
> 先把"病人"造出来，再验仪器。目前待执行。

---

## 第七部分：现在知道什么、不知道什么

**可以硬气说的（多版本复现 + 产物在案）：**

1. 端到端管线在两个平台有效；Q 测量跨机器一致到小数点后 4 位。
2. GRPO 早期伴随中深层激活谱收缩 8–13%（深层不恢复），跨执行环境、
   跨 probe 规模复现——pilot 最扎实的科学观察。
3. 在"0.5B + 200 步温和 GRPO + 50 步近邻任务适应"这个区间：16 次
   适应 15 正 1 零 0 负，**未观察到相对 ckpt-0 的一致性适应力下降**
   （个别 checkpoint 在个别 run 中低于 ckpt-0，但"是哪几个"每次都变）。
4. dormant fraction 在该区间零方差、无信息量（两平台复现）。
5. n=5 单种子的 Q–适应量相关**不稳定**：对执行环境和 Stage-B 种子
   都高度敏感（−0.60 / +0.50 / −0.50）。

**只能软说的（探索性，待下轮预注册检验）：**
6. centered anisotropy 与 delta 的相关符号在两条 run 一致——候选假设，
   不是发现。
7. GSM8K→SVAMP 直接迁移（Mac 起点 0.53→0.67）会通过天花板效应污染
   主 outcome——"SVAMP 太近"有数据实锤，但换任务家族是团队决策。

**还不知道的（exp 1.5 与正式实验要回答的）：**

- 加大剂量后可塑性损伤会不会真的出现？（MC1/MC2）
- 出现后 erank 能不能追踪它？（2×2 矩阵的裁决）
- dashboard 信号与 Q 谁预警更早？（正式实验的 detector bake-off）

**留给团队、不许静默决定的**：base vs Instruct、GRPO vs SFT 适应、
SVAMP 是否换成非数学家族（如 ProntoQA）、KL β>0 对照 arm 是否启用、
Colab 参考层是否要跑。

---

## 附录：数字速查表

| 量                               | Mac（版本 1）        | Win v1（版本 2，作废）  | Win v2（版本 3）             |
| ------------------------------- | ----------------:| ----------------:| ------------------------:|
| 训练 reward（首段→末段均值）              | 0.430→0.640      | 0.366→0.380（平）   | 0.354→0.582              |
| 权重最大相对变化（0→200）                 | 3.9e-6           | **2.4e-8（无效）**   | 6.06e-6                  |
| erank_L12 @ckpt-0               | 231.7567         | —（作废）            | 231.7567                 |
| erank_L12 @ckpt-200 vs 0        | +0.7%            | ≤0.14%（没训）       | −7.4%                    |
| erank_L22 @ckpt-200 vs 0        | −11.1%           | 同上               | −8.3%                    |
| dormant fraction                | 恒 0              | 恒 0              | 恒 0                      |
| SVAMP delta（0/25/50/100/200，pp） | +5/+13/+8/+2/+4  | ±2 内游走（无效）       | +6/+11/+3/+5/+12         |
| 主相关 rho（p）                      | **−0.60**（0.285） | −0.051（0.935，无效） | **+0.50**（0.391）         |
| seed 43 delta（描述性）              | —                | —                | +4/+8/+9/+6/+5，rho=−0.50 |
| seed 44                         | —                | —                | 仅 ckpt-0：0.00            |
| Stage A 用时                      | ~17 h（300 s/步）   | 3.09 h           | 5.18 h                   |
| 每次适应用时                          | —                | —                | ≈1 h                     |

## 附录：文档地图

| 文档                                                            | 内容                       |
| ------------------------------------------------------------- | ------------------------ |
| `eaaj-pilot/pilot_config.json`                                | 预注册配方（单一事实源）             |
| `agent_doc/AGENT_BRIEFING_first_experiment.md`                | Tommy spec + proposal 浓缩 |
| `eaaj-pilot/LOCAL_EXPERIMENT_PLAN.md`                         | 本地执行计划 + MPS 调查全文        |
| `eaaj-pilot-win4070/WIN4070_RUN_ANALYSIS.md`                  | v1 为何无效（判定证据链）           |
| `eaaj-pilot-win4070/WIN4070_V2_FINAL_REPORT_ZH.md`            | v2 完整报告                  |
| `eaaj-pilot-win4070/WIN4070_RERUN_GUIDE.md`                   | 重跑手册（含 step-25 关卡）       |
| `…/WIN4070_STAGEB_SEED_REPLICATION_PLAN.md` / `_STATUS_ZH.md` | 种子重复的计划与状态               |
| `PILOT_COMBINED_REPORT_3EXPERIMENTS(_ZH).md`                  | 三次执行 facts-only 综合报告     |
| `PILOT_TWO_RUNS_EXPLAINED_ZH.md`                              | Mac vs Win 差异深挖          |
| `PILOT_UNDERSTANDING_GUIDE_ZH.md`                             | 面向队友的讲解（有基础版）            |
| `experiment 1.5/EXPERIMENT_1_5_PLAN_ZH.md`                    | exp 1.5 预注册计划全文          |
| `experiment 1/`（evidence pack + zip）                          | 全部证据打包（已发 Slack）         |
| `eaaj-pilot/compute_log.md`                                   | 算力账本（Tommy 硬性要求）         |
