# 两次 Pilot（Mac CPU vs Windows CUDA v2）到底说明了什么

写给自己的完全解读版。日期：2026-07-12。

对应的两条 run：

| 简称 | 运行目录 | 执行环境 |
|---|---|---|
| **CPU run** | `eaaj-pilot/outputs/local_grpo_gsm8k_eac028bfcc87` | MacBook M3 Max，纯 fp32，普通 AdamW，micro-batch 8 × grad-accum 16→8×8 |
| **WIN run** | `eaaj-pilot/outputs/local_cuda_grpo_gsm8k_e9b0b52aab6c` | RTX 4070 Laptop，fp32 master + bf16 autocast，paged_adamw_8bit，micro-batch 4 × grad-accum 16 |

两条 run 用的是**同一份预注册配方**（`pilot_config.json`）：同一个模型 revision、同一个 seed 42、同 512 道 GSM8K、同 5 个 checkpoint、同一套冻结 probe、同一套 SVAMP 256 train / 100 eval / 50 updates。唯一被允许不同的是 **execution profile**（硬件、精度、optimizer 实现、批次几何）。

---

## 0. 一段话版本（TL;DR）

> 两次实验在"现象层"高度一致：训练都真实发生、中后层表征谱都收缩、五个
> checkpoint 的 SVAMP 适应全部为正提升、dormant fraction 全程为 0。
> 但在"统计层"完全不一致：主相关 rho(erank_L12, SVAMP delta) 一边是
> **−0.60**、一边是 **+0.50**，符号相反，都不显著。
> 造成差异的不是测量误差（ckpt-0 的 Q 值两边逐位一致），而是执行环境的
> 数值差异让 GRPO 采样出不同的回答，on-policy RL 把这点差异放大成两条
> 不同的训练轨迹——**等效于换了一次随机种子**。
> 所以共同结论是：管线和测量可信、谱压缩是可复现的真现象、没有观察到
> 塑性崩溃；而 n=5 单 seed 的相关系数被证明是噪声主导的——这正是
> 下一轮必须加独立 seed 的直接证据。

---

## 大白话版：这两次实验对整个 research proposal 意味着什么

### 它们是干什么用的？

先回忆 proposal 最终想证明的事：模型在做第一阶段 RL 训练时，只看它的
"脑内体检指标" Q，就能**提前**预知第二阶段训练会不会练不动（stall）——
就像体检指标预警疾病，比等到病发（reward 曲线躺平）早得多。

这两次本地实验（Mac + Windows）在整个计划里扮演的角色，说白了是三件事：

1. **验车。** 在花真钱（Colab 预算 / 团队资源）跑正式实验之前，把整条
   流水线——训练、存 checkpoint、测 Q、SVAMP 适应测试、统计分析——
   完整走通了两遍。现在可以拍胸脯说：车能开，仪表盘是准的（两台机器
   测同一个模型，Q 值小数点后四位一致）。
2. **排雷。** 提前踩出了好几个足以毁掉正式实验的地雷（下面细说）。
   最典型的是 Windows v1 的精度 bug：模型看着在训练，其实一个参数都
   没动。如果这个 bug 发生在正式实验里，我们会得出"Q 和适应性没关系"
   的**假结论**，而且根本不知道自己错了。
3. **白捡一次预演。** 两个平台恰好等于免费跑了"两个 seed"，提前告诉
   我们：5 个点、单 seed 算出来的相关系数完全靠不住（一边 −0.6、
   一边 +0.5）。这个教训如果等到正式实验才买到，价格是几十倍。

### 现在看出什么问题？（按严重程度排）

1. **最大的问题：我们想预警的"病"根本没出现。** proposal 要证明
   "Q 能提前预警 stall"，但两次实验里没有任何一个 checkpoint 出现
   stall——每个 checkpoint 学 SVAMP 都学得动、都是正提升。
   **病人没生病，就没法验证体检指标灵不灵。**很可能是 0.5B 小模型 +
   200 步温和训练（lr 极小、无 KL 惩罚）太轻了，不足以造成塑性损伤。
   正式实验必须先设计出能让 stall 真实出现的条件，否则整个"早期预警"
   的故事没有靶子。
2. **proposal 两个主打指标之一（dormant fraction）在这个设置下是废的。**
   全程为 0、纹丝不动，等于一个从不报警的烟雾探测器。要么调灵敏度
   （阈值），要么换指标。
3. **SVAMP 这个"第二阶段任务"选得太近了。** GSM8K 和 SVAMP 都是小学
   数学应用题，第一阶段学的东西直接搬过去就能用（Mac 那条 run 里
   SVAMP 起点从 0.53 一路涨到 0.67）。这会把"它还能学新东西吗"和
   "它早就会这个了"混在一起，污染主要结论。团队之前就担心这个，
   现在有数据实锤了。
4. **统计功效太低。** 5 个 checkpoint、1 个 seed，相关系数换台机器就
   变号。正式实验不加重复（多 seed），算出来的数字没有意义。
5. （工程层面）Windows 的断点修复和 8-bit optimizer 属于执行偏差，
   本地数字不适合直接当论文数字引用；要不要花 Colab 预算跑一条
   "干净参考版"，需要 Tommy 拍板。

### 好事还是坏事？

**总体是好事，而且是 pilot 能给出的最好结果之一。**

打个比方：花小钱做了一次新药的临床前预实验，结果是——器材全部好用、
测量方法靠谱、顺手还发现了一个真实的生理现象（谱收缩），同时暴露出
药效评估方案的三个漏洞。这不叫失败，这叫预实验的本职工作干得漂亮。
每个现在发现的问题都是省下来的钱：bf16 bug、dormant 失灵、SVAMP 太近、
单 seed 不可信——任何一个拖到正式实验才发现，代价都是几十倍。

坏消息也要诚实说：**"没观察到 collapse"对 proposal 是一个真实风险。**
它有两种解释，目前区分不了：

- 乐观解释：现象存在，只是这次训练太温和、任务太近，没把它逼出来
  → 正式实验加大 Stage-A 力度、换更远的任务就好；
- 悲观解释：在小模型/常规设置下这个现象本身就很弱甚至不存在
  → proposal 的核心假设需要修正。

区分这两种解释，正是下一轮实验该干的事——所以下一步的重点不是
"再跑一遍同样的东西"，而是加 seed、拉开训练强度、换更远的任务。

最后用一句好消息收尾：**谱收缩本身是个真发现。**两台机器独立复现、
换 2048 大 probe 也复现。就算最终"Q 预测 stall"不成立，"RLVR 早期
伴随中后层表征谱收缩"也是一个能写进 Research Doc、值得继续追的观察。

---

## 1. 先建立正确的心智模型：这两次实验是什么关系？

**不是**"一次对一次错"，**不是**"Mac 版和 Windows 版打架"。

正确的理解是：这是**从同一个实验设计里抽了两次样**。

GRPO 是 on-policy 强化学习：每一步都要让当前模型自己生成 8 个回答，然后按
reward 差异更新。生成是带温度采样（temperature 0.7）的随机过程。CPU 和
CUDA 的浮点运算顺序、精度（fp32 vs bf16 autocast）、随机数内核、批次切分
（8×8 vs 4×16）都不同——哪怕 seed 相同，**第 1 步采样出的 token 就已经不
一样了**。之后每一步都在"自己生成 → 自己更新"里滚动，微小差异指数放大。
到第 200 步，两条轨迹就是两个不同的模型。

> 类比：同一个班级、同一份教材、同一套考试，两个平行班各上一学期课。
> 教学大纲完全一样，但每堂课上学生说的话不一样，所以学到的东西有随机
> 差异。比较这两个班，你能分清"教材决定的部分"（两个班都出现的现象）
> 和"课堂随机性决定的部分"（只有一个班出现的现象）。

这个视角是整份文档的钥匙：**两条 run 都出现的东西 ≈ 设计的真实效应；
只有一条 run 出现的东西 ≈ 单次抽样噪声。**

一个重要的技术前提要先钉死：**测量尺子本身是跨平台一致的。**
两条 run 的 ckpt-0 就是同一个未训练的 base 模型，对它测出来的 Q 值：

| ckpt-0 指标 | CPU run | WIN run |
|---|---:|---:|
| erank_L4 | 225.14184 | 225.14177 |
| erank_L12 | 231.75671 | 231.75672 |
| erank_L22 | 354.19193 | 354.19189 |

小数点后四位一致。所以后面的一切分歧**都不是测量问题**，全部来自训练轨迹
本身的分岔。这本身就是一个有价值的工程结论：Phase-2 测量协议（冻结
probe、eval mode、固定 dtype）达到了预期的可比性。

---

## 2. 相同点：两条 run 都复现了什么

### 2.1 训练都真实发生（v2 修复后）

训练 reward 分段均值（exact-answer，训练采样分布上）：

| Stage-A steps | CPU run | WIN run |
|---:|---:|---:|
| 1–25 | 0.4300 | 0.3544 |
| 26–50 | 0.5175 | 0.4425 |
| 51–100 | 0.5747 | 0.4616 |
| 101–150 | 0.5816 | 0.5375 |
| 151–200 | 0.6400 | 0.5781 |

两边都稳定上升约 +0.22。权重 sentinel、weight-norm 变化也都确认更新真实
写进了参数（对照组：WIN v1 纯 bf16 是 no-op，最大权重变化只有 2.4e-8）。

### 2.2 中后层表征谱收缩：**这是两条 run 共同复现的核心现象**

erank 相对 ckpt-0 的变化：

| ckpt | CPU L12 | WIN L12 | CPU L22 | WIN L22 | CPU L4 | WIN L4 |
|---:|---:|---:|---:|---:|---:|---:|
| 25 | −3.4% | −7.1% | −9.1% | −9.3% | +0.1% | −2.6% |
| 50 | −5.3% | −8.6% | −13.5% | −8.1% | +0.0% | −0.9% |
| 100 | −1.0% | −9.9% | −12.1% | −10.5% | −0.5% | −1.3% |
| 200 | **+0.7%** | **−7.4%** | **−11.1%** | **−8.3%** | −0.4% | −0.8% |

共同模式：
- **Layer 4（浅层）基本不动** —— 两边一致。
- **Layer 22（深层）在前 25–50 步内快速收缩 8–13%，之后不再恢复** —— 两边一致，幅度都在同一量级。
- **Layer 12（中层）早期都收缩** —— 但结局不同（见 §3.2）。

WIN run 还做了 2048-probe 敏感性检查，方向一致，排除了 512-probe 秩上限
的假象。结论：**GRPO 训练早期伴随中后层激活谱收缩，这是一个跨执行环境
可复现的真实现象。**这是两次 pilot 最扎实的科学观察。

### 2.3 没有观察到固定预算适应性崩溃

十次独立的 50-update SVAMP 适应（2 条 run × 5 个 checkpoint），**delta 全部为正**：

| ckpt | CPU: before → after (delta) | WIN: before → after (delta) |
|---:|---|---|
| 0 | 0.53 → 0.58 (+5) | 0.53 → 0.59 (+6) |
| 25 | 0.48 → 0.61 (**+13**) | 0.51 → 0.62 (+11) |
| 50 | 0.61 → 0.69 (+8) | 0.56 → 0.59 (+3) |
| 100 | 0.63 → 0.65 (+2) | 0.55 → 0.60 (+5) |
| 200 | 0.67 → 0.71 (+4) | 0.54 → 0.66 (**+12**) |

没有任何一个 checkpoint 在 50 步预算内学不动 SVAMP。**"200 步 GSM8K GRPO
之后模型就丧失适应能力"这个最坏情形，在两条轨迹上都没有出现。**

### 2.4 dormant fraction 全程为 0 —— 两边一致

所有 checkpoint、三层、两个阈值（tau=0.025 / 0.1），dormant fraction 都是 0。
这个指标在"0.5B 模型 + 200 步温和 GRPO"这个区间里**没有方差，因此没有任何
预测能力**。这也是可复现的（负面）结果：要么换阈值/归一化重新预注册，
要么在这个 regime 放弃它作为早期预警信号。

### 2.5 一个值得注意的探索性巧合

探索性相关里，**centered anisotropy 的符号在两条 run 完全一致**：

| Q variant vs svamp_delta | CPU rho | WIN rho |
|---|---:|---:|
| aniso_c_L4 | −0.70 | −0.80 |
| aniso_c_L12 | +0.60 | +0.60 |
| aniso_c_L22 | +0.60 | +0.60 |

而 erank 的相关符号全部翻转（见 §3.3）。两次独立抽样中符号一致，比单次
n=5 的数字更值得注意——但它是事后在多个指标里挑出来的，**只能作为下一轮
预注册的候选假设，不能当作发现**。

---

## 3. 不同点：两条 run 在哪里分岔了

### 3.1 训练轨迹的"质量"不同：CPU 那条学得更多、迁移更强

| 量 | CPU run | WIN run |
|---|---|---|
| 训练 reward 水平 | 全程高 ~0.06–0.11 | 较低 |
| GSM8K held-out（64 题） | 0.44 → 0.52–0.55，**明显提升** | 0.36 → 0.42–0.47，噪声内波动 |
| SVAMP **before**（未适应就测） | 0.53 → **0.67**，随训练一路上涨 | 0.53 → 0.54，基本不动 |

这是最重要的分岔：**CPU 那条轨迹的 GSM8K 训练大量直接迁移到了 SVAMP**
（都是文字数学题），后期 checkpoint 还没开始适应就已经会做 67% 的 SVAMP；
WIN 那条轨迹几乎没有这种迁移。

（顺带：两边 ckpt-0 的 GSM8K 准确率差了 8 个百分点（0.4375 vs 0.3594），
同一个 base 模型！因为 eval 也是温度采样生成，64 题的单次评估本身就有
±6pp 量级的噪声——这提醒我们所有小 eval 数字都要放在噪声尺度下读。）

### 3.2 Layer-12 erank 的结局不同

两边 L12 都在前 50 步收缩，但 CPU run 到 ckpt-200 **完全恢复到基线之上**
（+0.7%），WIN run 停在 −7.4%。L22 倒是两边都不恢复。中层谱"收缩后是否
回弹"看起来本身就是轨迹相关的，不是设计的确定性质。

### 3.3 主相关符号相反 —— 全文最重要的"不同点"

| | CPU run | WIN run |
|---|---:|---:|
| **rho(erank_L12, svamp_delta)**（预注册主分析） | **−0.60**（p=0.28） | **+0.50**（p=0.39） |
| rho(erank_L4, svamp_delta) | +1.00 | +0.10 |
| rho(erank_L22, svamp_delta) | +0.20 | −0.10 |

同一设计、同一模型、同一数据、同一 seed 值，只换执行环境，主相关就从
"中等负"翻成"中等正"。如果我们只跑了其中任何一条，都可能讲出一个
方向截然相反、听起来头头是道的故事：

- 只看 CPU：「erank 越低 delta 越高？训练越多适应越差？塑性下降！」
- 只看 WIN：「erank 越高 delta 越高？Q 果然预测适应性！」

两条一起看，才知道**这两个故事都是在给噪声编剧情**。

---

## 4. 差异是什么造成的？（因果链，按置信度排序）

### 4.1 根本原因：执行数值差异 → 轨迹混沌分岔（置信度：高）

```
fp32 vs bf16 autocast、CPU vs CUDA 内核、8×8 vs 4×16 批次几何、RNG 流不同
        ↓  第 1 步生成的 completion 就不同
GRPO 是 on-policy：模型用自己的输出更新自己
        ↓  每一步都放大差异（混沌系统）
200 步后 = 两个不同的模型、两条不同的轨迹
        ↓
5 个 checkpoint 的 Q 排序不同、SVAMP delta 排序不同
        ↓  n=5 的 Spearman 只看排序，一两个点换位就能翻符号
主相关 −0.6 vs +0.5
```

关键：这**不是 bug，也不是哪边做错了**。这就是"换 seed"效应的样子。
两条 run 无意间构成了一次天然的 seed-robustness 检验，而主相关没有通过。

### 4.2 放大器：headroom/ceiling 效应污染了 CPU run 的主相关（置信度：中高）

CPU run 里后期 checkpoint 的 SVAMP before 已经涨到 0.63–0.67——**训练本身
把 Stage-B 的起点抬高了**。起点越高，50 步内可挤出的提升空间越小，所以
ckpt-100/200 的 delta 小（+2/+4），而 before 只有 0.48 的 ckpt-25 delta 最大
（+13）。Tommy 的 difficulty control（各自 baseline + 同套题）排除了"起点
更差所以显得提升多"，但**排除不了"起点太好所以没空间提升"**。

所以 CPU run 的 −0.60 很可能主要反映的是 **GSM8K→SVAMP 直接迁移造成的
天花板效应**，而不是塑性损失。WIN run 里 before 几乎不动，没有这个污染，
于是 delta 排序完全是另一副样子。这直接坐实了团队一直担心的
"SVAMP 离 GSM8K 太近"问题——现在有数据了。

### 4.3 次要因素：WIN run 特有的工程偏差（置信度：低，方向不明）

- WIN Phase 1 在 25/50/75 步断点修复过，optimizer moments 和 RNG state
  没有完整恢复——轨迹有效但不等价于不间断 run；
- `paged_adamw_8bit` 与 CPU 的 fp32 AdamW 数值行为不完全相同；
- bf16 autocast 前向本身有舍入差异。

这些让 WIN 轨迹"更不可复现"，但没有证据表明它们系统性偏向某个方向。
它们的作用更像是给 §4.1 的分岔再加了几个扰动源。

### 4.4 不是原因的东西（排除项）

- **不是测量管线的差异** —— ckpt-0 的 Q 逐位一致（§1）。
- **不是数据差异** —— 两边数据索引、probe、eval 题目都逐条相同（冻结在 config/manifest 里）。
- **不是 v1 那种精度 no-op** —— 两边 sentinel 全部通过，权重真实在动。

---

## 5. 能得出的共同结论

按"能说多硬"排序：

### 可以硬气说的

1. **端到端管线在两个平台上都有效，Q 测量跨平台可比。**
   （ckpt-0 逐位一致 + 33/33 sentinel 通过 + 全部产物可审计。）
2. **GRPO 早期伴随中后层（尤其 L22）激活谱收缩 8–13%，跨执行环境复现。**
   这是两次 pilot 最可信的科学观察，值得写进 Research Doc。
3. **在"0.5B + 200 步温和 GRPO (lr=1e-6, β=0) + 50 步 SVAMP"这个 regime 里，
   没有观察到固定预算适应性崩溃**：10/10 个 delta 为正。
4. **dormant fraction 在该 regime 下零方差、无信息量**（两边复现）。
5. **n=5 单 seed 的 Q–适应性相关是噪声主导的**：主相关符号在两次抽样间
   翻转。这是"下一轮必须加独立 seed"的最直接、最有说服力的证据。

### 只能软说的（探索性，供下轮预注册）

6. centered anisotropy 与 delta 的相关符号在两条 run 一致（L12/L22 为正、
   L4 为负）——候选假设，不是发现。
7. GSM8K→SVAMP 直接迁移强度本身就是轨迹相关的（CPU 强、WIN 弱），
   且一旦发生就会通过天花板效应污染主 outcome —— **SVAMP 作为 Stage-B
   任务确实"太近"，下轮需要一个非数学任务家族**（比如 ProntoQA）。

### 一句话综合结论

> **两次 pilot 共同证明：仪器是好的，现象（谱收缩）是真的，
> 但"谱收缩 ⇒ 塑性崩溃"没有被支持——因为 collapse 根本没有出现，
> 而当前设计（n=5、单 seed、近邻任务）的统计分辨率不足以检出
> Q 和适应性之间的任何真实关系。RQ1 保持开放。**

---

## 6. 对下一步的直接推论（每条都可溯源到上面的证据）

| 下一轮设计变更 | 由哪个观察直接推出 |
|---|---|
| ≥3 条 Stage-A seed × 每 ckpt ≥3 个 adaptation seed | §3.3 符号翻转：单 seed 相关不可信 |
| 增加非数学 Stage-B 任务家族（如 ProntoQA） | §4.2 天花板效应：SVAMP 太近，污染主 outcome |
| 预注册 curve-AUC / time-to-threshold 为次要 outcome | §2.3 曲线非单调 + 100 题 eval 单点 SE≈5pp |
| 更长/更强的 Stage-A（更多步数，或团队批准的 LR/β 对照） | §2.3 没有 collapse：可能是 Stage A 太温和，Q 与 outcome 都没拉开量程 |
| dormant 阈值重新预注册或弃用 | §2.4 零方差 |
| anisotropy 纳入下轮预注册的 Q 候选 | §2.5 跨 stratum 符号一致 |
| sentinel 作为所有正式 run 的硬 gate | WIN v1 的 bf16 no-op 教训 |

---

## 附：数据来源索引（全部可审计）

| 内容 | 路径 |
|---|---|
| CPU run 全部产物 | `eaaj-pilot/outputs/local_grpo_gsm8k_eac028bfcc87/` |
| WIN run 全部产物 | `eaaj-pilot/outputs/local_cuda_grpo_gsm8k_e9b0b52aab6c/` |
| 两边主表 / 相关表 | 各自 `analysis/results_table.csv`、`analysis/spearman_table.csv` |
| 两边主结果 JSON | 各自 `analysis/analysis_summary.json` |
| WIN v2 完整报告（中文） | `eaaj-pilot-win4070/WIN4070_V2_FINAL_REPORT_ZH.md` |
| WIN v1 为何无效 | `eaaj-pilot-win4070/WIN4070_RUN_ANALYSIS.md` |
| 计算账本与 deviation 记录 | `eaaj-pilot/compute_log.md` |
| 本文 reward 分段均值 | 由各自 `dashboard.jsonl` 的 `reward` 字段按 step 去重后计算 |
