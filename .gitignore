# OS / editor cruft
.DS_Store
.claude/
~$*
*.lock
.pytest_cache/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Python virtual environments
.venv/
venv/

# Slides / PPTs (not needed in the repo per team decision)
Slides/
*.pptx

# Model weights and optimizer/trainer state — too large for git, not
# needed since they're reproducible from pilot_config.json + checkpoints.
# Metrics, dashboards, configs, and logs under eaaj-pilot/outputs/ are kept.
*.safetensors
*.pt
*.pth
*.bin
tokenizer.json
stageb_repeat.lock

# Personal Chinese explainer for exp2 — intentionally not published
EXP2_UNDERSTANDING_GUIDE_ZH.md

# Build artifacts: the shareable run package (regenerable, 1.2 MB)
aaron_exp2_*.zip

# Selects the active run dir for eaaj-pilot phases 2-4; machine-local by
# design (CLAUDE.md) so per-machine active runs never collide in git.
eaaj-pilot/outputs/ACTIVE_RUN.txt
